services:
  - type: web
    name: mcp-server
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        fromService:
          type: web
          name: mcp-server
          property: port
    healthCheckPath: /health